- name: Demo on loops
  hosts: localhost
  vars:
    FRUITS: [ "apple", "frfbanana", "orange" ]
  tasks:
    - name: Print All fruits
      debug:
        msg: "Fruit Name = {{item}}"
      loop: "{{FRUITS}}"

# item is the variable given by ansible to use loop